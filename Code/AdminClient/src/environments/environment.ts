/**
 * Development environment settings
 */
export const environment = {
  production: false,
  logLevel: 'debug',
  useMockFormData: false // Set to true to use mock data that doesn't require a backend
}; 